export default function User() {
    return (
        <div><h1>user content</h1>
{/* 
            {userdata.users.map((user) => (
                <p key={user.id}>{user.firstName}</p>
            ))} */}

        </div>
    )
}